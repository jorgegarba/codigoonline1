// Supongando que tenemos la siguiente frase 

let frase = "Anita lava la tina";

// Crear una funcion que cumpla lo sguiente

/**
 * Función que retorna TRUE o FALSE, tras la 
 * evaluación del texto proporcionado y verificar 
 * que sea un palindromo
 * TEXTO QUE SE LEE DE IGUAL FORMA
 * DE IZQUIERDA A DERECHA y viceversa
 * @param {*} texto 
 */
let esPalindromo = (texto) => {

}

/**
 * Función que recibe una frase o texto  y 
 * retorna la cantidad de palabras que tiene
 * @param {*} texto 
 */
let contadorPalabras = (texto)=>{

}
