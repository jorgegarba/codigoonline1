import { createContext } from 'react';
// Siempre que se cree un context, se hace de la misma forma
// excepto que cambia el nombre del context
const RepartidorContext = createContext();

export default RepartidorContext;